import React from 'react';

/*
    Finds the last 10-15 read items and lists them in a side scrollable list.
    Items should have an X over one corner so they can be deleted from the list
        - All history should be saved and older items should move in as newer ones are deleted
        - Animate the slide in?
*/
const RecentReads = () => {
    return (
        <div>
            Recently Read Items
        </div>
    )
};

export default RecentReads;